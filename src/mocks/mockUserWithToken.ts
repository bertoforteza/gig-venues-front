import { UserLoginData } from "../redux/features/types";

const mockUserWithToken: UserLoginData = {
  id: "adminId",
  token: "adminToken",
  username: "admin",
};

export default mockUserWithToken;
